export enum StorageStatus {
    OFFLINE,
    STANDBY,
    RUNNING,
    FAULT,
    SLEEP_MODE
}

export enum StorageForcibleChargeDischarge {
    STOP,
    CHARGE,
    DISCHARGE
}

export enum MeterStatus {
    OFFLINE,
    ONLINE
}
